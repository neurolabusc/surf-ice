BEGIN
	RESETDEFAULTS;
	MESHLOAD('mni152_2009.mz3');
	OVERLAYLOAD('motor_4t95vol.nii.gz');
	OVERLAYMINMAX(1, 0.5, 2.5);

	//CLIPAZIMUTHELEVATION(0.3, 0, 130);
	SHADERAMBIENTOCCLUSION(0.5);
	COLORBARVISIBLE(true);


	//SHADERNAME('wire');
	SHADERFORBACKGROUNDONLY(true);
END.

